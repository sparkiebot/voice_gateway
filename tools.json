[
  {
    "name": "local_smalltalk",
    "description": "Classify one harmless Italian small-talk utterance after the wake word. Choose the intent evidenced by the utterance; do not use this for requests, questions needing context, or physical actions.",
    "gateway_local": true,
    "response_intent_argument": "intent",
    "parameters": {
      "type": "object",
      "properties": {
        "intent": {
          "type": "string",
          "enum": [
            "greeting", "wellbeing", "thanks", "goodbye", "identity",
            "capabilities", "compliment_response", "encouragement",
            "good_morning", "good_evening", "good_night"
          ],
          "description": "Exact small-talk intent expressed by the utterance."
        }
      },
      "required": ["intent"],
      "additionalProperties": false
    }
  },
  {
    "name": "get_battery_level",
    "description": "Propose reading the robot battery level.",
    "parameters": {"type": "object", "properties": {}, "required": [], "additionalProperties": false}
  },
  {
    "name": "navigate_to",
    "description": "Propose navigation to a named safe location.",
    "parameters": {
      "type": "object",
      "properties": {"location": {"type": "string", "minLength": 1}},
      "required": ["location"],
      "additionalProperties": false
    }
  }
]
